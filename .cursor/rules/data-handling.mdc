# Data Handling

## Types

- **NEVER use `any`** for API or form data – use proper types
- **Extract complex mapping** – If form data or API responses need transformation, extract to helper functions

## Form Data

- Use typed interfaces for form state
- Validate with Zod when submitting to API routes

## Scope

Applies to: Form handlers, API route handlers, content processing in `src/`
