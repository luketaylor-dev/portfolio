# File Naming

## Convention

- **ALWAYS use kebab-case** for file and folder names
- Example: `project-card.tsx`, `skill-card.tsx`, `mobile-nav.tsx`
- Never use PascalCase, camelCase, or snake_case

## Scope

Applies to: All files in `src/` and `content/`
